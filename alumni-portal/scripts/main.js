function requestMentorship(mentorName) {
    alert(`Mentorship request sent to ${mentorName}!`);
}

function loginUser(userType) {
    alert(`Logging in as ${userType}...`);
}

function submitProfile(userType) {
    alert(`${userType} profile updated successfully!`);
}
